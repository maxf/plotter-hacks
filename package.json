{
  "devDependencies": {
    "esbuild": "0.23.1",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "check": "npx tsc --noEmit",
    "build": "npx esbuild src/main.js --bundle --outfile=build/out.js",
    "watch-ts": "ls src/*.ts | entr npx tsc --noEmit",
    "watch": "ls src/* | entr npx esbuild src/main.js --bundle --outfile=build/out.js"
  },
  "dependencies": {
    "@types/delaunator": "^5.0.3",
    "@types/seedrandom": "^3.0.8",
    "delaunator": "^5.0.1",
    "seedrandom": "^3.0.5"
  }
}
